# 2026-08-03 开发日志

## 今日目标

收口 DeepSeek provider 支持与 smoke 发现的修复;新增主代理/执行层 Token 用量统计脚本。

## 今日进展

- 完成 OpenCode DeepSeek provider 支持(baseURL 白名单校验、Seatbelt `*:443` 四层收敛、`{env:}` 占位密钥不落盘、models.dev 缓存种入)。
- 修复 smoke 实测暴露的问题:external_directory 对无 `.git` 候选副本的误拦、Seatbelt `remote ip` 域名语法、沙箱内 DNS(mDNSResponder unix socket 被归为 network-outbound 遭拒,已在 profile 放行)。
- 修复 usage 脱敏误杀:落盘日志将 opencode step_finish 的 `tokens` 用量字典按密钥子树规则抹为 `[REDACTED]`,usage 解析改用脱敏前原始 stdout(仅提取数值字段,落盘脱敏不变),并补回归测试。
- A/B smoke 结论:qwen3-coder:30b(本地)零编辑失败;deepseek-v4-pro 一轮完成编码、一轮修复文档门禁,本地阶段实际扣费 $0.0159。质量提升成立;"省 token"假设不成立,真实收益是极低成本换可完成性与精确计量。
- 新增 `scripts/token_report.py`:扫描 Claude Code 转录与 OpenCode(宿主+各 run 隔离 runtime)SQLite,按北京时间分天输出主代理 token 与执行层 token/美元成本对比。

## 修改内容

- `src/mvp_orchestrator.py`:DeepSeek provider 运行时、Seatbelt 网络/DNS 规则、usage 原始 stdout 解析。
- `config/defaults.toml`:新增 `opencode_provider` 与 `[local.deepseek]` 段(默认仍为 ollama)。
- `tests/test_orchestrator.py`:provider/沙箱/usage 回归;修复 clean 环境下 opencode 裸名调用。
- `scripts/token_report.py`:新增 Token 用量统计脚本。

## 验证结果

- 原生回归 172 项通过(1 项沙箱内编译测试按预期跳过)。
- smoke B 组(deepseek)coder/fixer 均 exit 0,改动事务回写后目标单测通过;A 组(ollama)对照失败证据完整。
- `token_report.py --days 7` 与 `--json` 输出与手工核算一致。

## 后续事项

- 句子边界正则双向误判(M5 review 遗留)待修。
- summary 的 `model` 字段在 deepseek 路线下仍显示默认本地模型名,待修。
- 云端 codex CLI 401,待重新登录后跑完整闭环。
