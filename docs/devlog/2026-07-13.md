# 2026-07-13 开发日志

## 今日目标

将本地 AI MVP 工作流升级为低 token 的受监督开发流程，强制生成中文开发记录和 AI 可读项目文档，并在本地模型异常时由看门狗自动切换 Codex 接管。同时准备安全的 Git/GitHub 交付信息。

## 今日进展

- 将 Skill 默认输出从详细实时模式调整为简洁模式，Codex 对话只保留启动、心跳、Review、接管和最终状态。
- 增加本地模型看门狗：启动失败、命令失败、总超时或连续 300 秒没有结构化进展时，终止本地进程并调用 Codex 监督者接管。
- 为编码、修复、Review 和监督者提示词加入中文文档要求。
- 增加文档契约校验，缺少文档、中文内容不足、章节不完整或本次未更新都会阻止交付。
- 在 `summary.json` 中加入安全的版本管理快照，为用户验收后的 GitHub 发布做准备。

## 修改内容

- `src/mvp_orchestrator.py`：增加空闲超时、看门狗接管、中文文档校验和版本快照。
- `config/defaults.toml`：增加 `local_stall_timeout_seconds = 300`。
- `prompts/`：要求本地模型与 Codex 接管者同步维护中文日志、项目大纲和任务规划。
- `templates/AGENTS.md`：加入每次开发都要更新文档的仓库规则。
- `tests/test_orchestrator.py`：增加文档契约、停滞终止、接管、版本快照和遥测噪声测试。
- `~/.codex/skills/local-ai-mvp-builder/`：前序运行曾更新个人 Skill 工作流、文档规范和低 token 策略；本轮已由仓库内唯一维护源替代。

## 使用方法

前序运行曾使用个人目录脚本启动；该方式已被本轮仓库入口替代：

```bash
python3 ~/.codex/skills/local-ai-mvp-builder/scripts/run-supervised.py \
  --project "/项目绝对路径" \
  --plan "/计划绝对路径.md" \
  --model primary
```

需要在终端查看详细事件时增加：

```bash
--display live
```

用户验收后，可以明确要求“上传 GitHub”；此时才进入提交、推送分支和创建 PR 的流程。

## 验证结果

- 29 项单元测试全部通过。
- Python 编译检查、Shell 语法检查和 `git diff --check` 通过。
- Skill 格式校验通过。
- 故障注入确认本地模型错误会触发 Codex 接管，非结构化遥测输出不会错误刷新看门狗。

## 后续事项

- 等待用户评审本次工作流升级。
- 用户确认后生成正式提交信息，并按需推送到指定 GitHub 仓库或创建 Draft PR。
- 在首个真实 MVP 中继续观察 300 秒停滞阈值是否需要按项目调整。

---

## 跨 Agent 开源部署接管记录

### 今日目标

根据看门狗和两轮独立评审 findings，亲自修复不可解析的安装器、错误的监督启动门禁、无效的共享 Skill 包和虚假跨 Agent 测试，补齐 README、MIT License 与三份中文文档，使仓库达到公开评审状态。

### 今日进展

- 逐项复现安装器 Shell 语法错误、启动器帮助失败、Skill frontmatter 失效和真实入口缺少回归覆盖。
- 将安装器重构为薄 Shell 入口与可单测的 Python 安全实现，四个平台及 PATH 入口均链接到仓库权威源。
- 重写监督启动器参数解析与目标项目门禁，支持从任意工作目录和入口软链接运行。
- 按公共 Agent Skills 子集重建 Skill，加入计划/文档参考文件和 Codex UI 元数据，删除 `.bak` 副本。
- 用临时 HOME、临时 Git 项目和真实仓库入口重写 12 项跨 Agent 回归测试。
- 完成跨平台安装、升级、卸载、备份恢复、安全边界和后端角色说明，并增加 MIT License。
- 新的独立 Review 已通过，自动运行遗留的失败回滚测试与日志证据问题均已关闭，没有新的 P0/P1/P2 finding。
- 已将仓库唯一 Skill 源实际部署到 Codex、Claude Code、Antigravity IDE 和 Antigravity CLI，并安装统一 PATH 启动器；旧 Codex Skill 已保留时间戳备份。
- 已按用户明确授权创建公开仓库 `https://github.com/mintandkiwi/local-ai-mvp-builder`，推送 `main` 基线和 `agent/cross-agent-skill` 功能分支，并创建 Draft PR `https://github.com/mintandkiwi/local-ai-mvp-builder/pull/1`。

### 修改内容

- `bin/install-agent-skills`、`src/agent_skill_installer.py`：实现四平台映射、dry-run、幂等链接、默认冲突拒绝、`--force` 原子备份与失败回滚、软链接父目录防护和部分失败汇总。
- `bin/mvp-loop-supervised`：解析 `--project`、`--plan`、`--model`、`--display`，拒绝 `--allow-dirty`，使用 `git status --porcelain` 检查目标项目，再运行 doctor 并调用 `mvp_orchestrator.py run`。
- `integrations/skills/local-ai-mvp-builder/`：成为唯一 Skill 维护源，包含有效首行 frontmatter、两份参考文档和 `agents/openai.yaml`。
- `tests/test_cross_agent_integration.py`：直接覆盖真实安装、冲突、备份、软链接、任意 cwd、参数映射、缺失文件和未跟踪脏工作区。
- `tests/test_orchestrator.py`：仅在 exit 71 且明确返回父级 Seatbelt 禁止嵌套的错误时标记动态沙箱用例跳过；其他错误仍失败。
- `README.md`、`LICENSE`：补齐开源部署、平台差异、回滚与许可说明。

### 使用方法

先在不写文件的模式查看全部目标：

```sh
./bin/install-agent-skills --dry-run
```

确认后安装，并保证用户命令目录在 PATH 中：

```sh
./bin/install-agent-skills
export PATH="$HOME/.local/bin:$PATH"
```

执行批准计划：

```sh
mvp-loop-supervised \
  --project "/项目绝对路径" \
  --plan "$HOME/.local/share/local-ai-mvp-builder/plans/项目-plan.md" \
  --model primary \
  --display summary
```

普通冲突先人工核对；确需替换时使用 `--force`，并保留安装器生成的时间戳备份用于恢复。

### 验证结果

- 自动受监督运行最终输出 `needs_manual_attention`：最终 Review 发现共享 Skill 的非 Codex 接管边界、空白验证命令门禁和文档状态仍需修复。
- 人工接管后，`python3 -m unittest discover -s tests -v` 在原生环境中 44 项全部通过；新增用例确认空字符串和纯空白验证命令会在 doctor/coder 启动前被拒绝，并通过故障注入验证链接创建失败时恢复原 Skill。
- 同一测试命令通过受监督验证沙箱运行时 44 项通过、2 项精确跳过；跳过项分别需要父级 Seatbelt 不允许的嵌套 `sandbox-exec` 和 SIGKILL，静态安全策略及原生动态用例仍已覆盖。
- `python3 -m compileall -q src`：通过。
- `sh -n bin/mvp-loop bin/mvp-loop-supervised bin/install-agent-skills bin/pull-models`：通过。
- 14 项新增真实跨 Agent 回归测试全部通过，包含强制注入软链接创建失败并确认原 Skill 内容完整恢复；测试未访问真实 `~/.codex`、`~/.claude` 或 `~/.gemini`。
- Skill frontmatter/包结构回归与 Ruby YAML 解析通过；使用临时 PyYAML 依赖运行官方 skill-creator `quick_validate.py`，输出 `Skill is valid!`。
- 独立只读 Review 最终 verdict 为 `pass`，完整 diff 未发现新的 P0/P1/P2。
- 实际部署核验通过：四个平台的 `SKILL.md` 均可读且软链接到仓库唯一源，`~/.local/bin/mvp-loop-supervised --help` 正常，Claude Code 2.1.207 与 Antigravity IDE 均已确认存在。

### 后续事项

- 保留原自动运行的 `needs_manual_attention` 作为历史证据；本段“独立 Review 已通过”仅指随后开始 Token 效率整改前的跨 Agent 部署基线，不代表下文整改 diff 已通过终审。
- 由用户评审 GitHub Draft PR #1；确认后再决定是否转为 Ready、合并或发布版本，本轮不自动合并、不创建 tag/release。
- 在首个真实 MVP 中验证四种 Agent 前端的技能发现与长任务接管闭环。

---

## Token 效率整改接管记录

### 今日目标

根据看门狗接管和独立评审 findings，亲自完成 TE-001 至 TE-009：建立可审计 Token 归因和扩展 summary，在模型修改前阻断环境/基线失败，按风险和 finding 严重度自适应路由，压缩并脱敏后续上下文，同时保留测试、安全、中文文档和独立终审门槛。TE-010 真实 A/B 未获预算授权，本轮不调用真实模型做对照。

### 今日进展

- 逐项复现并确认全部 findings 成立：原代码只有 live Token 显示、旧 summary、coder 后验证、固定两轮 review、完整计划/结果重复嵌入，Skill/README/AI 文档也仍是旧流程。
- 实现 JSON exact、文本 partial、缺失 unavailable 的 Token 解析和 agent 阶段归因；失败/超时不再按 0，summary 可给出 local、cloud-review、cloud-supervisor、cloud lower bound 和完整性。
- 增加模型前 reviewer/Seatbelt 宿主检查和目标只读干净基线；失败分类为环境或基线代码/测试问题，且不会启动 local/cloud agent。
- 将默认流程改为 adaptive：low/medium local-first，high/未声明 direct-cloud；验证失败先本地定向修复，首轮严重 finding 立即接管，只有局部 P2 才进入第二轮。
- 增加去敏限长 context capsule、云端软预算/最大调用数、finding/验证/接管统计和 `no_baseline` 证据约束。
- 更新唯一 Skill 维护源、plan/evidence reference、UI 元数据、四类提示词、README 和 CLI 帮助。
- 自动 final reviewer 因云端额度耗尽未生成 `review-3.json`，运行保持 `needs_manual_attention`；随后由当前 Codex 会话进行人工恢复，修复 Token 错归因、合法非对象 JSON 崩溃、持久化日志脱敏、high 风险 doctor 误阻断、接管验证失败仍调用终审和 capsule 截断证据丢失等问题。
- 云端能力恢复后执行独立只读 `review-4`；评审没有修改文件，复现并报告 1 个 P1、5 个 P2，状态继续为 `needs_manual_attention`。随后修复 quoted JSON/YAML 与加密私钥泄漏、JSON primitive 事件崩溃、极限 capsule 丢失验证/文件范围、模糊错误文本误判环境、仅检查命令存在的 reviewer 预检，以及 P3-only 无效云端接管。
- 修复后执行独立只读 `review-5`，再次得到 2 个 P1、5 个 P2：脱敏先于 JSON 解析导致 Token telemetry 被误删、嵌套/camelCase 凭据漏出、嵌套 usage 类型未防御、Git porcelain 首路径被 `.strip()` 破坏、完整验证日志无哈希、缓存/临时目录环境识别不足，以及云端调用上限可能耗尽强制终审容量。上述 7 项均已按根因修复并补端到端测试，仍待新的独立复审。
- `review-6` 再报告 2 个 P1、5 个 P2：占位符/冲突风险声明可误降为 low、空格分隔 CLI secret 泄漏、可选 usage 字段畸形仍记 exact、应用自返 126/127 被误判环境、正常/compact capsule 未强制验哈希、旧备份 Skill 仍可被发现，以及失败 agent 真实退出码丢失。上述 7 项已修复。
- 跨日执行的 `review-7` 报告 2 个 P1、3 个 P2：授权头/多词/额外 CLI 凭据仍可能泄漏、空失败或畸形 finding 可被提升、损坏的最终 usage 事件会复用早先值、真实子进程归因矩阵不足，以及缺少新日期中文日志。上述问题已在 7 月 14 日修复并记录，等待 `review-8`。

### 修改内容

- `src/mvp_orchestrator.py`：新增 Token parser/aggregator、风险解析、显式 review 决策、前置基线、阶段跟踪、summary 契约、capsule 和预算状态；修复局部 fixer/接管验证失败后仍可能继续 review、旧文本日志引用其他 JSON usage 时错归因、所有 JSONL/评审根节点的非对象防御、quoted/multiline 凭据和各类私钥块脱敏、阶段感知失败分类、reviewer 版本/能力/仓库读取预检、P3-only 非阻塞归一化，以及极限 capsule 的独立 scope/review 证据与哈希引用。
- `review-5` 接管补丁：持久化证据改为先解析完整 JSON/逐行 JSONL 并递归清除敏感子树，纯文本正则只作兜底，数值 usage 保持 exact；新增 `git status --porcelain=v1 -z` 解析、完整 validation manifest 及逐日志 SHA-256、临时/缓存写 probe、编译器临时文件环境分类，以及 supervisor + final review 两次云端容量预留。
- `review-6` 接管补丁：风险只接受非代码块内恰好一条完整声明，其余保守 high；任意文本和结构化 command 均脱敏 `--api-key value`/`--password value`；所有出现的 usage 字段必须是非负真整数才记 exact；126/127 必须伴随 shell/toolchain 证据；agent 调用前程序化验证 plan/scope/manifest/每个日志/review 哈希；失败通过 `CommandError`/`StructuredResultError` 保留真实退出状态。
- `src/agent_skill_installer.py`：`--force` 和旧版 discoverable backup 统一迁移到 `~/.local/share/local-ai-mvp-builder/backups/<平台>/`。已实际把当前 Codex 旧备份迁出发现目录，四平台活跃链接保持仓库唯一源。
- `config/defaults.toml`：新增 adaptive/legacy、严重 finding 阈值、capsule 上限、soft Token budget 和 cloud call 上限。
- `tests/test_orchestrator.py`、`tests/test_cross_agent_integration.py`：增加 Token、summary、预检零调用、风险/调用序列、watchdog、capsule、日志/JSON 脱敏、doctor 风险分流和新 Skill 契约回归。
- `prompts/`、`integrations/skills/local-ai-mvp-builder/`、`README.md`、`bin/mvp-loop-supervised`：改为风险路由、按需上下文和可审计用量说明。
- `docs/ai/PROJECT_OUTLINE.md`、`docs/ai/TASK_PLAN.md`：记录新数据流、隐私边界、TE-001 至 TE-010 状态和 A/B 授权门禁。

### 使用方法

批准计划必须加入精确风险行，例如：

```text
Risk classification: low
```

仍通过统一入口运行；模型前基线失败不会启动任何 agent：

```sh
mvp-loop-supervised --project "/项目绝对路径" --plan "/计划绝对路径.md" --model primary --display summary
```

运行后检查 `summary.json` 的 `usage.stages`、`usage.cloud_lower_bound`、`usage.measurement_complete`、`workflow.takeover`、`workflow.validation_counts`、`workflow.finding_counts` 和 `efficiency.claim`。没有完整 direct-Codex 对照时，`efficiency.claim` 应保持 `no_baseline`。

### 验证结果

- 本次确实执行了一次受监督开发运行，但不是 TE-010 A/B：两次本地调用均为 exact，分别为 641,306/4,358 和 852,967/5,449 个输入/输出 Token，本地合计 1,504,080 Token；两次本地模型均声称完成但没有落盘。
- 旧流程共发起 4 次云端调用：review-1 partial 95,030、review-2 partial 73,155、supervisor partial 243,511、final-review unavailable；已知云端下限为 411,696 Token，计量不完整。接管原因是旧流程两轮 review 失败阈值，最终失败分类为 reviewer 工具/额度不可用，`efficiency.claim` 只能是 `no_baseline`。
- supervisor 初始实现后的受管验证共 60 项，其中 58 项通过、2 项按父级 Seatbelt 限制精确跳过；Python 编译和中文文档契约通过。自动 final reviewer 因使用额度耗尽未执行，不能记为独立终审通过。
- 第一轮人工恢复后 `python3 -m unittest discover -s tests -v`：68 项全部通过；原生环境没有跳过或失败。
- 修复独立 `review-4` 的 6 个阻断 finding 后，同一全量命令增至 76 项并全部通过；新增用例覆盖 quoted/多行/现代凭据、加密私钥、命令参数脱敏、JSON primitive、1024-byte validation-only capsule、阶段化分类、损坏/不兼容 reviewer 可执行文件和 P3-only 路由。
- 修复独立 `review-5` 的 7 个阻断 finding 后，全量命令增至 86 项并全部通过；新增端到端用例确认 exact usage 可穿过 `run_command` 和 `tracked_agent` 进入 summary、嵌套/camelCase 凭据不落盘、bool/malformed usage 不崩溃、porcelain `-z` 保留所有路径、成功/失败验证日志篡改可由哈希发现、cache/temp probe 先于基线命令，以及 1/2/3-call 配置不会让 supervisor 后缺失 final review。
- 新增 cache/TMP probe 首次真实 Seatbelt 运行发现登录 shell 内相对 `mkdir` 不可用（exit 127）；改为 `/bin/mkdir` 与 `/bin/rm` 后，真实 probe 和只读最小基线均为 exit 0。该问题未被 mock 掩盖，已计入最终验证证据。
- 修复 `review-6` 后，全量回归增至 89 项且全部通过；新增占位/重复/冲突/代码块风险的实际 command_run 路由、CLI flag/value 多 sink 脱敏、全部可选 usage 类型、应用与 shell 126/127 对照、normal/compact/final capsule 篡改、旧备份四平台迁移和任意非零/结构化失败退出码覆盖。
- 修复跨日 `review-7` 后，全量回归增至 93 项且全部通过；新增授权头/多词密码/refresh token/ID token/Cookie 脱敏、空失败与严格 schema、损坏最终 usage 事件以及真实 fake Codex 退出归因矩阵。
- `python3 -m compileall -q src`、`bash -n bin/mvp-loop bin/mvp-loop-supervised bin/install-agent-skills`、`./bin/mvp-loop-supervised --help` 和 `git diff --check`：通过。
- 官方 Skill Creator `quick_validate.py` 使用本机临时 PyYAML 依赖运行，输出 `Skill is valid!`；未联网安装或修改校验器。
- `review-4.json` 至 `review-14.json` 均为 `fail`；十一轮共 14 个 P1、44 个 P2 已完成针对性修复并有回归，最新状态与补丁记录见 7 月 14 日日志。`review-15` 因 usage limit 未产生 verdict，运行状态继续保持 `needs_manual_attention`。

### 后续事项

- 修复后的 `review-15` 独立 reviewer 因 usage limit 未产生 verdict；云端能力恢复后重建证据并重试，通过前不宣称 `ready_for_user_review`。
- 本轮不提交、不推送、不切换分支、不改写历史。
- TE-010 保持待用户单独批准云端 Token 预算；批准前不得把本次整改描述为“Token 节省已验证”。
