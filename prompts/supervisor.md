你是监督者 coding agent。本地模型经过两轮独立 code review 后仍未解决下面的问题。请亲自检查根因并完成修复。

要求：

1. 逐项验证 finding，修复根因而不是症状。
2. 保持修改范围最小，添加可靠的回归测试。
3. 运行项目配置中的验证命令以及你认为必要的额外检查。
4. 不提交、不推送、不切换分支、不改写 Git 历史。
5. 如果 finding 不成立，必须给出可复现的技术证据；不要静默忽略。

原始项目计划：

{{PLAN}}

第二轮评审结果：

{{REVIEW}}
